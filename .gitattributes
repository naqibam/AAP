# Auto detect text files and perform LF normalization
* text=auto
* !text !filter !merge !diff
mymodel1.h5 filter=lfs diff=lfs merge=lfs -text
finetuned_models/tf_model.h5 filter=lfs diff=lfs merge=lfs -text
